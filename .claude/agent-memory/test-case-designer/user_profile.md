---
name: User Profile
description: User practices algorithm problems in TypeScript, writes exhaustive test files, does not write solution code themselves in this workflow
type: user
---

The user is working through algorithm/data structure problems as programming exercises. They write test files first (or alongside) implementations. They are comfortable with TypeScript, Jest, and standard algorithm problem patterns (two-pointer, palindrome checks, etc.). They provide precise problem descriptions including function signatures and expect test cases that cover all edge and boundary conditions rigorously.
